import { DirectiverandomcolorDirective } from './directiverandomcolor.directive';

describe('DirectiverandomcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiverandomcolorDirective();
    expect(directive).toBeTruthy();
  });
});
